Simple xDAQ application that connects to a DB, methods to access the values and 
insert new ones are implemented using tstore client


source oracle environment
source /afs/cern.ch/project/oracle/script/setoraenv.sh


###################################

0.  src/common/HelloTStore.cc change   

  request.addTStoreParameter("credentials","castaned/XXXXX");  // user database change for your username/password
  request.addTStoreParameter("credentials","GEMDB/XXXXX");  //GEMDB change for your username/password


1.- Compilation 

    make  (first need to set the environment variables as in any xdaq app)

2.- Execution

    export TSTORE_XML=$PWD/views/   (View files define which table we will look at)
    xdaq.sh  -p 12345 -c $PWD/xml/hellotstore.xml -e $PWD/xml/hellotstore.profile

3. In the browser 

   Once inside the browser

   http://gem904daq01.cern.ch:12345 (enter tstore application and select the query method)


4.- Different view can be configure, so far views/ directory include calls to VFAT2 configuration table

